<h1>New Feedback</h1>

<%= form_with model: @feedback, class: 'feedbacks__form' do |form| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
        <% @feedback.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="feedbacks__button">
    <%= form.submit "J'envoie ma demande d'évaluation", class: 'pix-button pix-button--shape-rounded pix-button--size-big pix-button--background-blue' %>
  </div>
<% end %>

<%= link_to 'Back', feedbacks_path %>
